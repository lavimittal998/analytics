# comment to explain your code
mtcars # control + enter
?mtcars to get help
?mean
class(mtcars)
summary(mtcars)
?summary # summary of data base
str(mtcars) 
table(mtcars$gear)
table(mtcars$carb)
table(mtcars$gear, mtcars$cyl)
?class
